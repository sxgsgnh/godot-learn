target("modules")
    set_kind("static")

    -- Include 路径
    add_includedirs(".")
    add_includedirs("..", {public = true})

    -- ========================================================================
    -- 模块源文件
    -- ========================================================================

    add_files(
        "**/register_types.cpp",
        "**/**.cpp"
    )

    -- ========================================================================
    -- 编译定义
    -- ========================================================================

    add_defines("MODULES_ENABLED")

    -- ========================================================================
    -- 依赖
    -- ========================================================================

    add_deps("core", "servers", "scene")

    set_targetdir("$(buildir)/lib")

target_end()
